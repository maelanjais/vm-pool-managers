apiVersion: 1

providers:
  - name: CloudPoolManager
    orgId: 1
    folder: CloudPoolManager
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
